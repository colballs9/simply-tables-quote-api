# Database — local development
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/simply_tables

# Database — Cloud SQL (via Unix socket)
# DATABASE_URL=postgresql+asyncpg://postgres:PASSWORD@/simply_tables?host=/cloudsql/PROJECT:REGION:INSTANCE

# Debug
SQL_ECHO=false
